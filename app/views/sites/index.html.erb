<div class="container">
	<p class="text-center">
		<% if(current_user) %>
			<%= link_to 'Companies', companies_path, class: 'btn btn-primary' %>
			<%= link_to 'People', people_path, class: 'btn btn-primary' %>
		<% else %>
			<%= link_to 'Login', login_path, class: 'btn btn-primary' %>
		<% end %>
	</p>
</div>